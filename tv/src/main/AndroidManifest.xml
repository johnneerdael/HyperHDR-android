<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!-- Required to dispatch the package-install intent for the in-app updater. The user
         must additionally grant "Install unknown apps" for this app in system settings;
         we redirect them there if needed. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:banner="@drawable/banner"
        android:icon="@drawable/hyperhdr_square"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hyperhdr_square"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        tools:targetApi="33">
        <!-- Activities, services, receivers populated in Plan 3 -->
        <service
            android:name="eu.hyperhdr.android.service.HyperHdrCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <receiver
            android:name="eu.hyperhdr.android.service.HyperHdrBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <service
            android:name="eu.hyperhdr.android.service.HyperHdrTileService"
            android:exported="true"
            android:icon="@android:drawable/ic_media_play"
            android:label="HyperHDR"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>

        <activity
            android:name="eu.hyperhdr.android.tv.ui.wizard.WizardActivity"
            android:exported="false"/>

        <activity
            android:name="eu.hyperhdr.android.tv.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="eu.hyperhdr.android.tv.ui.settings.SettingsActivity"
            android:exported="false"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updateprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
