<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="lollipop">
        <service
            android:name="com.hyperion.grabber.common.HyperionScreenService"
            android:exported="true"
            android:foregroundServiceType="mediaProjection">
            <intent-filter>
                <action android:name="com.hyperion.grabber.common.HyperionScreenService" />
                <action android:name="com.hyperion.grabber.service.ACTION_START" />
                <action android:name="com.hyperion.grabber.service.ACTION_STOP" />
                <action android:name="com.hyperion.grabber.service.ACTION_STATUS" />
                <action android:name="com.hyperion.grabber.service.ACTION_EXIT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".HyperionGrabberBootReceiver"
            android:exported="true"
            android:label="HyperionGrabberBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity android:name=".BootActivity" android:theme="@style/Theme.Transparent" android:exported="true"/>
    </application>

</manifest>